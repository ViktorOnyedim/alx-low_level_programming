#include "main.h"

/**
 * add - adds two integers
 * @x: First integer to add
 * @y: Second integer to add
 *
 * Return: result
 */
int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
